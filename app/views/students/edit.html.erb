<h3>Edit your name</h3>
<%= form_for @student do |f|%>
  <%= f.label "What's your new first name?"%></br>
  <%= f.text_field :first_name%></br>

  <%= f.label "What's your new last name?"%></br>
  <%= f.text_field :last_name%></br>
  <%= f.submit %>
<%end%> 